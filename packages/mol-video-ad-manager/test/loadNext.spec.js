// eslint-disable-next-line no-unused-vars
import loadNext from '../src/loadNext';

test('loadNext must throw if you don\'t pass a valid vastResponse');
test('loadNext must return the new vastChain');
test('loadNext must track whatever error may have happened');
test('loadNext must accept an optional track function to track the error');
